<% if flash.any? %>

	<div id="flash-message">

		<% flash.each do |key, value| %>

			<div class="flash flash-<%= key %>">
				<%= value unless value.blank? %>
			</div>

		<% end %>

	</div>

<% end %>