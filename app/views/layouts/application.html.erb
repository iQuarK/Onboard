<%= render 'layouts/header' %>

<body class="l-application">

	<header class="top-bar">

		<div class="content-wrapper">

			<%= link_to 'pinpoint', admin_url(subdomain: @company.subdomain), class: 'logo' %>

			<nav class="main-nav">
				<ul>
					<li><%= link_to "Dashboard", admin_url(subdomain: @company.subdomain) %></li>
					<li><%= link_to "Settings", admin_company_url(subdomain: @company.subdomain) %></li>
					<li><%= link_to "Logout", destroy_user_session_path, method: :delete %></li>
				</ul>
			</nav>

		</div>

	</header>

	<%#= render 'shared/flash_message' %>

	<%= yield %>

</body>

<%= render 'layouts/footer' %>
